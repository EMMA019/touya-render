# Situation art (web / API)

Required stills: `{characterId}/{situationId}.png`.

Optional looping card video (same stem):

```
public/situations/hiyori/cafe-rain.mp4
```

Point `situations[].video` at `/situations/hiyori/cafe-rain.mp4` in `shared/characters/hiyori.json`. The file is not required for catalog validation or CI.

See [android/OZ_SITUATION_VIDEO.md](../../android/OZ_SITUATION_VIDEO.md).
